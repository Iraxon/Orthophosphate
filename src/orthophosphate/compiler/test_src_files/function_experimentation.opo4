f(x 1 2)

f
    x 0
    y b(2)
    z
        1
        2

def tag fn

    list
        param1 tag_type str
        param2 name str
        param3 content List(str)

    cast TagFile(tag_type)
        file name content

def tag fn(f [x] sum(x 1))

def inc fn(int int sum(_ 1))

# def function list(cmd)


def tag fn
    [type name content]
    cast TagFile(type) file name content

# test(thing)(thing2)

# sum(. 1)
#    4
